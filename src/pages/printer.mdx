import { Link as GatsbyLink } from 'gatsby';
import { Grid, Card, Box, Divider, Heading, Link, Text } from 'theme-ui';

import Printer from '../components/printer';
import BackButton from '../components/back-button';

<BackButton />

# Printer

Using the `print` media query it's also possible to display a different image when the webpage is printed / is viewed in print preview.

The `src` for this example can be found <Link href="https://github.com/PaulieScanlon/gatsby-plugin-image-with-art-direction/blob/main/src/components/printer.js" target="_blank" rel="noopener">here</Link>

<Grid
  sx={{
    gridTemplateColumns: 'auto 1fr',
    gap: 2
  }}
>
  <span aria-label="Warning">⚠️</span>
  <Text sx={{ color: 'red', fontWeight: 'bold' }}>
    This functionality is currently only supported by Firefox
  </Text>
</Grid>

<Divider />

<Card variant="cards.secondary">
  <Printer />
  <Box variant="cards.body">
    <Heading as="h2" variant="styles.h2">
      Printer
    </Heading>
    <Text as="div" sx={{ mb: 2 }}>
      Photos by <Text sx={{ fontWeight: 'bold' }}>Todd Quackenbush</Text> and{' '}
      <Text sx={{ fontWeight: 'bold' }}>Markus Spiske</Text>
    </Text>
    <Grid
      sx={{
        gap: 0
      }}
    >
      <Link
        href="https://unsplash.com/@toddquackenbush"
        target="_blank"
        rel="noopener"
        variant="styles.a"
      >
        https://unsplash.com/@toddquackenbush
      </Link>
      <Link
        href="https://unsplash.com/@markusspiske"
        target="_blank"
        rel="noopener"
        variant="styles.a"
      >
        https://unsplash.com/@markusspiske
      </Link>
    </Grid>
  </Box>
</Card>

<Divider />
